# Changelog

## 0.1.0 — 2026-02-23

Initial release.

- Sidebar TreeView showing MCP servers across all scopes (project, global, local, managed, plugin)
- One-click enable/disable toggle with checkboxes
- Two-tier health checks: lightweight auto-checks on refresh, deep MCP SDK handshake on demand
- Plugin discovery from Claude Code plugin registry
- Edit config file in-place from context menu
- Claude Code environment settings (Tool Search Mode) in the sidebar
- Extension settings for health check behavior and timeout
